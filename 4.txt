import React from 'react';
import { Product } from '../types';
import { ShieldCheck, TrendingUp, Clock } from 'lucide-react';

interface CoinCardProps {
  product: Product;
  onInvest: (product: Product) => void;
}

export const CoinCard: React.FC<CoinCardProps> = ({ product, onInvest }) => {
  return (
    <div className="bg-slate-900/90 border border-slate-800 hover:border-amber-500/40 transition-all rounded-3xl p-5 space-y-4 flex flex-col justify-between shadow-xl relative overflow-hidden group">
      <div className="absolute -top-12 -right-12 w-32 h-32 bg-amber-500/5 rounded-full blur-2xl group-hover:bg-amber-500/10 transition-all pointer-events-none" />

      <div className="space-y-3">
        <div className="relative w-full h-44 rounded-2xl overflow-hidden bg-slate-950 border border-slate-800 flex items-center justify-center p-2 group-hover:border-slate-700 transition-all">
          <img
            src={product.image}
            alt={product.name}
            referrerPolicy="no-referrer"
            className="w-full h-full object-cover rounded-xl transform group-hover:scale-105 transition-transform duration-300"
          />
          <div className="absolute top-3 left-3 bg-slate-950/80 backdrop-blur-md px-2.5 py-1 rounded-full border border-amber-500/30 text-[10px] font-bold text-amber-400 flex items-center space-x-1">
            <ShieldCheck className="w-3 h-3 text-amber-400" />
            <span>{product.tag}</span>
          </div>
          <div className={`absolute top-3 right-3 px-2.5 py-1 rounded-full text-[10px] font-bold border ${
            product.risk === 'High Risk'
              ? 'bg-rose-500/20 text-rose-400 border-rose-500/30'
              : 'bg-emerald-500/20 text-emerald-400 border-emerald-500/30'
          }`}>
            {product.risk}
          </div>
        </div>

        <div>
          <h3 className="text-lg font-black text-white group-hover:text-amber-400 transition-colors flex items-center justify-between">
            <span>{product.name}</span>
          </h3>
          <div className="flex items-center space-x-1 text-emerald-400 font-bold text-xs mt-1">
            <TrendingUp className="w-3.5 h-3.5" />
            <span>{product.returnRate}</span>
          </div>
          <p className="text-xs text-slate-400 mt-1.5 leading-relaxed">
            {product.description}
          </p>
        </div>

        <div className="flex items-center space-x-2 text-[11px] text-slate-400 bg-slate-950/60 p-2 rounded-xl border border-slate-800/80">
          <Clock className="w-3.5 h-3.5 text-amber-400" />
          <span>Locking Period: <strong className="text-slate-200">{product.lockPeriod}</strong></span>
        </div>
      </div>

      <div className="pt-3 border-t border-slate-800/80 flex items-center justify-between">
        <div>
          <span className="text-[10px] uppercase tracking-wider text-slate-500 block font-semibold">Min Deposit</span>
          <span className="text-lg font-black text-white">{product.price}</span>
        </div>
        <button
          onClick={() => onInvest(product)}
          className="px-5 py-2.5 rounded-xl bg-gradient-to-r from-amber-500 to-amber-600 hover:from-amber-400 hover:to-amber-500 text-slate-950 font-black text-xs shadow-lg shadow-amber-500/20 hover:scale-105 active:scale-95 transition-all cursor-pointer flex items-center space-x-1.5"
        >
          <span>Invest Now</span>
        </button>
      </div>
    </div>
  );
};
